function fibonacci(n) {
    var total = 0;
    // base case
    if(n === 0) {
        return total += n;
    } else if (n === 1) {
        return total += n;
    } else { // recursive case
        return total = fibonacci(n-1) + fibonacci(n-2)
    }
    console.log(total)
}
